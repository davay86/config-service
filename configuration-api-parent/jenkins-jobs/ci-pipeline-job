def pomLocation = "configuration-api-parent"

node {
  def img

  stage('Checkout') {
    checkout scm
  }

  stage('Build and Release'){
    sh "mvn -B -V -U -e -f " + pomLocation + "/pom.xml release:clean release:prepare -Pdocker-publish"
  }

}