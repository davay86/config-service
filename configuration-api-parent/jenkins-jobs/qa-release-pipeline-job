node{
    stage('Release to QA'){
        sh "kubectl rolling-update config-api [config-api] --image=sleepingtalent/configuration-api:latest"
    }

    stage('Verify QA'){
        //verify QA manual or smoke test.
    }
}