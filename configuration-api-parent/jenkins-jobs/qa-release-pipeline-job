node{
    stage('Release to QA'){
        sh "kubectl set image deployment config-api config-api=sleepingtalent/configuration-api:latest --record"
    }

    stage('Verify QA'){
        //verify QA manual or smoke test.
    }
}